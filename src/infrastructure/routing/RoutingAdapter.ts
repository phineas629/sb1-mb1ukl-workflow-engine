export interface RoutingAdapter {
  // Define methods for routing operations
  // For example:
  // registerRoute(path: string, handler: Function): void;
  // handleRequest(request: any, response: any): Promise<void>;
}
